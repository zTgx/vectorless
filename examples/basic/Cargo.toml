[package]
name = "basic-example"
version = "0.1.0"
edition = "2024"

publish = false

[dependencies]
vectorless-core = { path = "../../core" }
vectorless-llm = { path = "../../llm" }
async-trait = { workspace = true }
tokio = { workspace = true }
serde = { workspace = true }
toml = "0.8"
